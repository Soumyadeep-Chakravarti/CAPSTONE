---
created: 2026-07-06
modified: 2026-07-06
type: project
tags:
  - projects
  - enki
  - session-notes
status: seedling
---
# ENKI Session — 2026-07-06

## Goal
- Set up distrobox development environment for ENKI
- Implement gesture classifier (pure geometry)

## Progress
### Done
- Created `enki-dev` distrobox from `archlinux:latest`
- Installed base packages: rust, cargo, clang, cmake, pkg-config, git, openssl
- Set rustup default to stable (1.96.1)
- Verified `cargo build` passes in distrobox
- Verified `cargo test` passes in distrobox (10 tests, all passing)
- Updated bootstrap script at `scripts/setup-enki-dev.sh`
- Updated docs with two-layer workflow (distrobox for code, host for hardware)

**Gesture classifier (pure geometry):**
- Implemented `PoseClassifier` in `src/gesture/classifier.rs`
- Implemented `GestureStateMachine` in `src/gesture/state.rs`
- Detected gestures: OpenPalm, Fist, Point, Pinch, Peace, Spread
- Unit tests: 10 tests covering all gesture types and state transitions

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- ENKI uses two-layer development: distrobox for code, host for hardware
- Gesture classifier is pure geometry (no timing, no state)
- State machine handles lifecycle (started/held/ended events)
- ML feature stays gated behind `--features ml`
- TFLite build deferred (heavy compile, not blocking current work)

## Next Steps
- Wire classifier into orchestrator pipeline
- Improve MockCamera with realistic landmark sequences
- Verify behavior against LoggingInput
- Add uinput output
- Replace mock ML with TFLite
- Replace mock camera with V4L2

## Relevant Files
- `scripts/setup-enki-dev.sh` — bootstrap script
- `docs/ENKI.md` — updated with distrobox workflow
- `agent.md` — updated with development environment
- `src/gesture/classifier.rs` — pure geometry classifier (PoseClassifier)
- `src/gesture/state.rs` — state machine (GestureStateMachine)
- `src/gesture/mod.rs` — updated to expose both

---
↗ Up: [ENKI](../ENKI.md)

## See also
  - [[2026-05-25|Session: 2026-05-25]]
  - [[active|Active Threads]]
  - [[continuity|Continuity]]
